#include <stdio.h>
//The void there before has a capital letter at the beginning so I've change it
int main(void)
//Theres no opening bracket so I've put it one.
{
    //int before was all caps so I've change it all in lowercase
    int sum;
    //The comment below doesn't have '*/' or the first symbol doesn't look like this '//' so I've change it
    // COMPUTE RESULT

    //Before the line before doesn't have a semicolon after it
    sum = 25 + 37 - 19;

    //The comment below before doesn't have the correct format
    /* DISPLAY RESULTS */

    //There's no comma after the quotation before
    printf("The answer is %i\n", sum);
    return 0;
}